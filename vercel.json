{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd apps/mobile && npm install --legacy-peer-deps --no-audit --no-fund --loglevel=error && npx expo export --platform web --output-dir dist",
  "outputDirectory": "apps/mobile/dist",
  "framework": null,
  "installCommand": "echo skip",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}

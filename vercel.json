{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "corepack enable && corepack prepare pnpm@9.15.5 --activate && pnpm install --no-frozen-lockfile && pnpm --filter @shakana/mobile build:web",
  "outputDirectory": "apps/mobile/dist",
  "framework": null,
  "installCommand": "echo skip",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
